{% extends '::base.html.twig' %}

{% block title %}Add a admin{% endblock %}

{% block body %}
<div class="width-300 center-block">
  <h1 class="text-centered">Add a admin</h1>
  {{ form_start(form) }}
    {{ form_rest(form) }}
    <input type="submit" class="btn btn-primary btn-block margin-top-5" value="Save"/>

    {{ form_end(form) }}
    <a class="btn btn-primary btn-block margin-top-5" href="{{ path('admin_list') }}">Admin list</a>

</div>
{% endblock %}
